{
  "name": "ivypoledance-booking",
  "private": true,
  "type": "module",
  "description": "Booking API for ivypoledance.at: course dates, places and waitlist",
  "scripts": {
    "test": "node --test test/booking.test.mjs test/api.test.mjs",
    "deploy": "wrangler deploy",
    "schema": "wrangler d1 execute ivypoledance-booking --remote --file schema.sql"
  },
  "engines": {
    "node": ">=22"
  }
}
